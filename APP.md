# アプリ企画

- アプリID: A008
- 名前: 防災ホームベース
- 対象ユーザー: 平時の備えと災害時の行動をスマートフォンで迷わず確認したい人・家族
- 解決する困りごと: 備蓄、持ち出し品、保管場所、家族情報、災害別手順、保険情報が分散し、平時に見直せず、緊急時に「あれはどこ」「どうすればよい」が分からなくなる
- バージョン: v2（2026-08 リニューアル。v1からの全面書き換え）
- 今回学ぶ技術（1つ）: 平時と緊急時で情報優先度を切り替える状態機械、および合言葉ベースの家族間データ共有

## 主要ユーザーフロー

1. 初回はホームの準備率0%から始まり、「今日やること」が家族カードの作成（呼び名・集合場所）を提案する。案内どおりに登録すると、次は世帯設定（大人・子どもの人数、備蓄日数）の入力を促す。
2. 世帯設定を入れると、備蓄の目安（14品目 × 世帯人数 × 備蓄日数の必要量）が台帳の備蓄タブに表示される。「おすすめセットをまとめて追加」で未登録分を一括登録すると、準備率が上がり「今日やること」の内容も次の不足へ切り替わる。
3. 台帳で備蓄・持ち出し品を追加・編集し、数量、必要数、期限、保管場所、持ち出し対象を管理する。期限が近い・切れた品目は「入れ替え」タブに一覧され、「消費した」を押すと端末ローカルの買い足しリストへ移る。
4. 手順画面で11災害・5段階を検索・絞り込みし、よく使う手順をお気に入りにする。お気に入りは再読み込み後も保持される。
5. 台帳内の保険タブで加入状況、補償確認、証券の保管場所、更新日、最終確認日を記録し、根拠付きの見直し候補（未確認・1年超未確認・更新30日以内・補償未確認）を確認する。
6. 全画面共通の「緊急モード」から災害を選び、今すぐ行う手順（now→after）、持ち出し品、集合場所、家族の配慮事項、緊急連絡メモを表示する。alert/recoverが存在しない災害（停電・火災など）ではその見出し自体を出さない。
7. 緊急手順の確認状態（チェック）は保存され、再読み込み後も続きから再開する。安全確認後は確認ダイアログを経て平時へ戻る。
8. （任意）家族カード画面から合言葉を発行し、他端末で「合言葉で参加」すると、家族カード・持ち出し品・保険メモなどが端末間で自動的に同じ内容になる（買い足しリストは端末ローカルのまま共有されない）。

## 主要機能

1. 平時・緊急モード切り替え（11災害対応）
2. 準備台帳（持ち出し品・備蓄・入れ替え〈ローリングストック〉・保管場所・保険の5タブ）
3. 備蓄の目安の自動計算（世帯人数 × 備蓄日数）
4. 災害別・段階別手順の検索・絞り込み・お気に入り
5. 家族カードと集合場所、世帯設定
6. 家族共有（合言葉→Firestore、任意・無料・未設定なら完全ローカル動作）
7. PWA（ホーム画面追加・オフライン起動）

## 画面構成

- **ホーム**: 準備率（水・食料・電源照明・衛生トイレ・情報書類・家族連絡の6カテゴリ、fraction平均のパーセント表示）、今日やること（不足の優先順位に沿って1件提案）、点検リマインダー（防災の日など）、期限・不足の注意カード。
- **準備台帳**: 5タブ構成。
  - 持ち出し品（goBag）: 持ち出し対象に指定した品目。
  - 備蓄（stock）: STOCK_GUIDE 14品目の必要量と保有量の比較、不足分の一括追加。
  - 入れ替え（rolling）: 期限が近い・切れている備蓄の一覧（期限昇順）。「消費した」で買い足しリストへ。
  - 保管場所（locations）: 場所名・メモの登録、備蓄・持ち出し品からの参照。
  - 保険（insurance）: 加入状況、地震・風水害・家財の補償確認、証券保管場所、更新日、最終確認日、見直し候補。
- **手順を探す**: 検索語・災害フィルター・段階フィルター・お気に入り絞り込み、結果0件からの回復導線。
- **家族カード**: 家族の呼び名・連絡メモ・集合場所・配慮事項（アレルギー・服薬等）、世帯設定（大人・子ども人数、備蓄日数、緊急連絡メモ）、共有設定（合言葉の発行・参加・共有停止）。
- **緊急モード**: 災害選択、今すぐ行う手順（now→after、チェック可能・リロード後も保持）、持ち出し品、集合場所と配慮事項、緊急連絡メモ、171（災害用伝言ダイヤル）の案内。

## データ構造

- 保存先: `localStorage` の `a008.disaster-home-base.v2`。家族共有を有効にしていない限り外部通信は行わない。
- `schemaVersion`: 2 固定。旧キー `a008.disaster-home-base.v1` が残っていて v2 キーが無い場合は起動時に自動移行する（`migrateV1`。旧手順ID→現行IDの対応表つき。対応が一意に定まらない3件のみ意図的に非対応）。
- `AppState`: `schemaVersion`、`household`、`supplies`、`locations`、`familyMembers`、`insurance`（以上5つが家族共有の対象＝`SHARED_KEYS`）、`shopping`（端末ローカル、共有しない）、`mode`、`selectedHazard`、`emergencyCheckedIds`、`favoriteProcedureIds`、`dismissedReminders`、`ui`、`sync`。
- `Household`: 大人人数、子ども人数、備蓄日数（既定3日）、緊急連絡メモ、更新日時。
- `Supply`: ID、品名、カテゴリ、数量、単位、最小数量、期限、保管場所ID、持ち出し対象、`recommendedKey`（STOCK_GUIDE品目との対応。手入力品は null）、更新日時。上限200件。
- `Location`: ID、場所名、メモ、更新日時。上限30件。
- `FamilyMember`: ID、呼び名、連絡メモ、集合場所、配慮事項、更新日時。上限20件。
- `Insurance`: 加入状況、地震・風水害・家財の確認状態、証券保管場所、更新日、最終確認日、メモ、更新日時。
- `Procedure`: コード内固定データ（`js/data/procedures.js`、107件）。ID、災害（11種）、段階（5種のうち該当するもののみ）、見出し、本文、検索語。保存するのはお気に入りIDのみ。
- `STOCK_GUIDE`（`js/data/stock-guide.js`）: 水・食料・電源照明・衛生・書類の14品目。各品目は `perPerson`（人数倍）・`perDay`（日数倍）・`rate` を持ち、`requiredQuantity(item, household)` が世帯人数×備蓄日数から必要量を切り上げ計算する。
- `sync`: `enabled`・`passphrase`・`householdId`（合言葉から生成した64桁16進数。SHA-256）。householdIdが不正な形式なら保存されていた `enabled` に関わらず強制OFFにする。
- 準備率、期限警告、検索結果、保険の見直し候補、備蓄の必要量は派生値として毎回計算し、保存しない（`js/derive.js`）。

## 家族共有の仕組み

- 合言葉（ひらがな4語＋4桁、約49ビット）から SHA-256 で世帯ID（64桁16進数）を導出し、Firestore匿名認証でその世帯のドキュメントにアクセスする。
- 無料の Firebase Sparkプラン固定（クレジットカード登録不要）。設定手順は [docs/firebase-setup.md](./docs/firebase-setup.md)。
- `js/firebase-config.js` の `firebaseConfig` が `null` の間は共有機能そのものが動かず、外部通信は一切発生しない（Service Workerも `googleapis.com` を素通りさせるのみで、未設定時に事前キャッシュしない）。
- 共有対象は `household/supplies/locations/familyMembers/insurance` の5キーのみ。`shopping`（買い足しリスト）・`mode`・`emergencyCheckedIds`・`ui` などは端末ローカルのまま。
- 競合解消は `mergeEntities`（`js/sync-logic.js`）が `updatedAt` の新しい方を優先するLast-Write-Wins。片方のみ存在する項目は残す。オフライン編集はFirestore SDK自身のオフライン永続化に任せ、Service Workerはこの通信を一切キャッシュ・改変しない。

## 状態遷移

- 起動: `booting → restored | migratedFromV1 | empty | recoveryNotice`。v2データが壊れている場合は安全な既定値へ正規化（`safeState`）し、可能な範囲でユーザーに通知を出す。
- 平時: `normal.home ↔ supplies(5タブ) ↔ procedures ↔ family`。
- 緊急開始: `normal → hazardSelecting → emergency.active`。災害未選択では開始しない。
- 緊急中: `active → procedureChecked`。同じ項目の確認は冪等。災害変更時は確認後にチェックをリセットする。alert/recover段階の手順が0件の災害では、その見出しごと表示しない。
- 緊急終了: `emergency.active → exitConfirm → normal.home`。キャンセルで緊急画面へ戻る。
- 編集: `idle → editing → validating → saving → saved | saveError`。保存成功後だけ状態を確定し、失敗時は入力を保持して再試行する。
- 共有: `sync.disabled → creating | joining → sync.active`。合言葉の発行・参加後は自動的にリモートとローカルをマージし続ける。householdIdの検証に失敗すれば `sync.disabled` へ強制的に戻す。

## 完成条件

- [x] 11災害を選んで平時と緊急を切り替え、緊急手順（now→after）・持ち出し品・集合場所を表示できる。alert/recoverが無い災害ではその見出しを出さない。
- [x] 備蓄・持ち出し品を登録・編集し、数量、必要数（世帯人数×備蓄日数で自動計算）、期限、保管場所、持ち出し対象を確認できる。
- [x] 期限が近い・切れた備蓄を「入れ替え」で確認し、「消費した」で端末ローカルの買い足しリストへ移せる。
- [x] 家族カードと保管場所を登録・編集し、緊急画面には必要な情報だけを大きく表示できる。
- [x] 災害別手順（107件）を検索・絞り込み・お気に入り登録でき、検索結果0件から条件を解除できる。お気に入りは再読み込み後も保持される。
- [x] 保険確認を保存し、未確認、確認から1年以上、更新30日以内、災害補償未確認を根拠付きで案内できる。
- [x] 状態を端末内保存し、再読み込み復元、v1データの自動移行、破損データ回復、保存失敗後の入力保持・再試行ができる。
- [x] 家族共有（合言葉→Firestore匿名認証）を任意で有効化でき、未設定時は外部通信ゼロで完全ローカル動作する。無料のSparkプランに固定されている。
- [x] PWAとしてホーム画面に追加でき、オフラインで全機能が開く。Service Workerのキャッシュバージョンはデプロイごとに上げる運用。
- [x] 初回・空・入力エラー・保存中・保存失敗を備え、キーボード、読み上げ、動き軽減に対応できる。
- [x] 360px・768px・1280pxで主要導線が破綻せず、外部通信（未設定時）、仮データ、未接続ボタン、TODOがない。

## テスト計画

- [x] 派生値（準備率6カテゴリ、今日やること、期限・不足、点検リマインダー、保険見直し候補、手順検索）を純粋関数として検証（`tests/derive.test.js`）。
- [x] 11災害・107手順のデータ整合性（ID重複なし、災害ごと8件以上、now段階2件以上、文字数上限、全件100件以上）を検証（`tests/procedures.test.js`）。
- [x] v2状態モデルの正規化・上限（備蓄200/保管場所30/家族20/買い足し100）・v1移行・破損データ回復・保存失敗時の挙動を検証（`tests/state.test.js`）。
- [x] 備蓄の目安14品目・必要量計算（人数×日数）の境界値を検証（`tests/stock-guide.test.js`）。
- [x] 合言葉の生成・正規化・世帯ID導出・エンティティのマージ（Last-Write-Wins）を検証（`tests/sync-logic.test.js`）。
- [x] 入力バリデーション（数量境界、品名文字数、世帯人数、日付妥当性など）を検証（`tests/validate.test.js`）。
- [x] index.htmlの必須要素・PWA/アクセシビリティ基本・公的指示優先の文言・TODOなしを検証（`tests/ui-static.test.js`）。
- [x] テストランナー自体の疎通確認（`tests/smoke.test.js`）。
- 現在93/93件PASS（`npm test`）。

## 今回は作らないもの

- 気象庁等の外部API、現在地・地図・GPS、プッシュ通知、避難所検索、災害速報、クラウド同期以外の安否送信手段、アカウント登録・パスワード認証。
- 保険商品の比較、価格、契約勧誘、補償額診断、個別の法務・金融助言。表示内容は一般的な確認の目安に限定する。
- 証券番号全文、住所、生年月日、写真、PDFの保存。バーコード、在庫自動消費、独自手順エディター、データ書き出し・取り込み。
- 買い足しリストの端末間共有（意図的に端末ローカルのみ）。
